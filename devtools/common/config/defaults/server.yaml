# Default config for @aweave/server (NestJS)
# User override: ~/.aweave/config/common/server.yaml
# Sensitive values (AUTH_TOKEN) must use environment variables — not this file.

configVersion: 1

server:
  port: 3456
  host: "127.0.0.1"

database:
  debate:
    dir: "~/.aweave/db"
    name: "debate.db"
  docstore:
    path: "~/.aweave/docstore.db"
